<div class="width-container">
  <h1>Products</h1>

  <p><small> <%= page_entries_info @products %></small></hp>

    <%= form_tag products_path, method: :get, authentication: false, id: 'search-form' do %>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search" %>
    <% end %>
  
  <div id ="products">
    <%= render @products %>
  </div>


  <%= paginate @products %>
</div>